{
  "name": "enterprise-app-backend",
  "version": "1.0.0",
  "description": "Backend API for Enterprise Deployment Platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --coverage --coverageReporters=lcov --coverageReporters=text --reporters=default --reporters=jest-junit",
    "lint": "eslint ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.4.1"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "jest-junit": "^17.0.0",
    "nodemon": "^3.1.3",
    "supertest": "^7.0.0"
  },
  "jest-junit": {
    "outputDirectory": "./",
    "outputName": "test-results.xml"
  }
}
